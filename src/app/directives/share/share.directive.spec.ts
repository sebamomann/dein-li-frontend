import {ShareDirective} from './share.directive';

describe('ShareDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareDirective(null);
    expect(directive).toBeTruthy();
  });
});
